#include "jparse.h"

int
main (int argc,
      char **argv)
{
  int nread = 0;
  char source[1000];
  if (argc != 2)
    return 1;

  while (!feof (stdin))
    nread += fread (source, 1, 999, stdin);
  source[nread] = '\0';
  json_data_t *data = json_decode (source);

  
